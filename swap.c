#include "monty.h"

/**
 * swap - This swaps the two elements on top of the stack.
 * @command: This is data being accessed from the command struct.
 *
 * Return: Void.
 */

void swap(command_t *command)
{
	(void)command;
}
